server:
  port: 5002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yt_airline_db
    username: root
    password: Shiva2001@
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true